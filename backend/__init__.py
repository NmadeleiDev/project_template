"""
This application follows layered architecture pattern.

Structure:
- api: API layer (FastAPI application).
- domain: Domain layer (implementation of the domain logic).
- repository: Repository layer (database operations).
- service: Service layer (connection between the API layer and the domain layer).
"""
